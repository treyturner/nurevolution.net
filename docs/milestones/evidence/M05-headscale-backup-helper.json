{
  "checks": [
    "empty repository produces explicit empty reports",
    "real restic 0.19.1 + rclone init, encrypted backup, full check, exact restore after service restart",
    "existing and live-directory restore destinations refused",
    "FAIL_STOP: service running, failed capture cleaned, accepted snapshot unchanged",
    "FAIL_COPY: service running, failed capture cleaned, accepted snapshot unchanged",
    "FAIL_UPLOAD: service running, failed capture cleaned, accepted snapshot unchanged",
    "repository failure leaves running service untouched",
    "intentionally stopped service stays stopped",
    "existing capture preserved without stopping service",
    "snapshot and dry-run retention JSON match through pipes and a real PTY despite a silent TTY renderer",
    "missing report output and restic failures produce visible errors and fail the helper",
    "explicit retention marker allows scoped forget and prune"
  ],
  "restic": "restic 0.19.1 compiled with go1.26.4 on linux/amd64",
  "rclone": "rclone v1.69.3-DEV",
  "limits": "Lifecycle fault injection uses a Docker command fixture; repository encryption, rclone transport, backup and restore use real clients. Live Google Drive and Headscale restore remain owner acceptance.",
  "at": "2026-09-11T19:29:43.312249+00:00",
  "isolatedServer": {
    "version": "0.29.3",
    "network": "none",
    "healthy": true,
    "serverKeysPreserved": true,
    "restoreFilesUnchanged": true,
    "fixtureEnrollmentKeysRecovered": 1,
    "fixtureUsersRecovered": 1,
    "liveNodesTested": false,
    "repeatableCheck": {
      "command": "sudo python3 deploy/headscale/check-restore.py",
      "requiredCiJob": "headscale-backup",
      "at": "2026-09-11T20:22:50.702289+00:00",
      "passed": true,
      "scope": "Runs the actual check-restore.sh against a populated fixture; restic encryption and file restoration are exercised separately by check-backup.py."
    }
  },
  "liveDestination": {
    "remote": "gdrive.fracturetrey",
    "rcloneConfig": "/boot/config/plugins/rclone/.rclone.conf",
    "repository": "rclone:gdrive.fracturetrey:Headscale/restic",
    "status": "Owner verified initial backup and independent restore; weekly User Script and retention marker configured."
  },
  "terminalRegression": {
    "reproduced": "Pinned helper and restic 0.19.1 exit successfully with no snapshots/retention output on a TTY when RESTIC_PROGRESS_FPS=0.016666 is global; non-TTY output works.",
    "fix": "Format piped restic JSON as plain JSON for snapshots and retention; confirm empty selections and fail visibly on missing/failed report data. Progress throttling remains limited to the captured backup log.",
    "test": "Real PTY and pipe reports must match the accepted snapshot ID, host, tag, time, retention policy, and keep/remove selections even when the restic wrapper suppresses TTY reports.",
    "passed": true,
    "firstCorrection": "Scoping progress throttling passed locally but the owner confirmed reports remained silent after a checksum-verified update.",
    "at": "2026-09-11T20:12:02.796187+00:00",
    "liveConfirmation": "Owner confirmed both JSON reports with the actual Google Drive snapshot and retention selection."
  }
}
